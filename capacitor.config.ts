import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  "bundledWebRuntime": true,
  appId: 'com.example.app',
  appName: 'pos-application',
  webDir: 'dist'
};

export default config;
